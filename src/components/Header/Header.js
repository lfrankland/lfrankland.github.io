import React from 'react'
import styles from './Header.module.css'

const Header = () => (
	<header className={styles.Header}>
		<h2 className={styles.title}>Louis Frankland</h2>
	</header>
)

export default Header
